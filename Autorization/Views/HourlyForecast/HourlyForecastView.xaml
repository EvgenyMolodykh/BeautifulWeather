<UserControl x:Class="WeatherAppWPF.Views.HourlyForecast.HourlyForecastView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WeatherAppWPF.Views.HourlyForecast"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:converters="clr-namespace:WeatherAppWPF.Resources.Converters"
             Padding="5">
    <UserControl.Resources>
        <converters:WeatherCodeToIconConverter x:Key="WeatherCodeToIconConverter"/>
        <converters:WindDirectionConverter x:Key="WindDirectionConverter"/>
        <converters:PressureConverter x:Key="PressureConverter"/>
    </UserControl.Resources>

    <Border Style="{StaticResource BorderHourlyForecastView}">
        <Grid>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource HourlyForecastView}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{DynamicResource Time}" Style="{StaticResource HourlyForecastViewStyles}"/>
                    <TextBlock Grid.Column="1" Text="{DynamicResource Temp}" Style="{StaticResource HourlyForecastViewStyles}"/>
                    <TextBlock Grid.Column="2" Text="{DynamicResource Pressure}" Style="{StaticResource HourlyForecastViewStyles}"/>
                    <TextBlock Grid.Column="3" Text="{DynamicResource Precipitation}" Style="{StaticResource HourlyForecastViewStyles}"/>
                    <TextBlock Grid.Column="4" Text="{DynamicResource Direct}" Style="{StaticResource HourlyForecastViewStyles}"/>
                    <TextBlock Grid.Column="5" Text="{DynamicResource Speed}" Style="{StaticResource HourlyForecastViewStyles}"/>
                    <TextBlock Grid.Column="6" Text="{DynamicResource Weather}" Style="{StaticResource HourlyForecastViewStyles}"/>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1"
                          HorizontalScrollBarVisibility="Disabled"
                          VerticalScrollBarVisibility="Hidden"
                          Background="White">
                <ItemsControl ItemsSource="{Binding Path=HourlyForecasts}" Margin="1,0">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Vertical"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Path=Time, StringFormat={}{0:HH:mm}}" HorizontalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="{Binding Path=Temperature}" HorizontalAlignment="Center"/>
                                <TextBlock Grid.Column="2" Text="{Binding Path=ApparentTemperature}" HorizontalAlignment="Center"/>
                                <TextBlock Grid.Column="3" Text="{Binding Path=RelativeHumidity}" HorizontalAlignment="Center"/>
                                <ContentControl Grid.Column="4" HorizontalAlignment="Center">
                                    <Viewbox Width="20" Height="20" Stretch="Uniform">
                                        <ContentControl Template="{Binding WindDirection, Converter={StaticResource WindDirectionConverter}}"/>
                                    </Viewbox>
                                </ContentControl>
                                <TextBlock Grid.Column="5" Text="{Binding Path=WindSpeed}" HorizontalAlignment="Center"/>
                                <ContentControl Grid.Column="6" HorizontalAlignment="Center">
                                    <Viewbox Width="20" Height="20" Stretch="Uniform">
                                        <ContentControl Template="{Binding Path=Weather, Converter={StaticResource WeatherCodeToIconConverter}}"/>
                                    </Viewbox>
                                </ContentControl>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>